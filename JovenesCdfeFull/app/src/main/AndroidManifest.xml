<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ec.edu.epn.jovenescdfefull">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".Cancionero" />
        <activity android:name=".Calendario" />
        <activity android:name=".Ensenanzas" />
        <activity android:name=".PublicacionesFacebook" />
        <activity android:name=".TusVersiculos" />
        <activity android:name=".Musica" />
        <activity android:name=".Videos" />
        <activity android:name=".IngresarFechas"></activity>
    </application>

</manifest>